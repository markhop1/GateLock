# Include runtime dependencies required by tests (e.g. cv2, numpy, insightface).
-r requirements.txt

# Testing dependencies
pytest>=7.4.0
tensorflow-datasets>=4.9.0  # For download_lfw_fixtures.py (LFW face images)
pytest-cov>=4.1.0
pytest-mock>=3.11.1
responses>=0.24.0  # Para mock de requests HTTP

# Development tools
black>=23.0.0  # Code formatter (opcional)
flake8>=6.0.0  # Linter (opcional)
